# Components

Use this directory when the project has meaningful component boundaries.

A component may justify its own tests and documentation when it has clear responsibility, interfaces, dependencies, and independent evolution.

Component-local tests belong under:

```text
components/<component>/tests/
```
